// import React from "react";

const PointsBalance = ({transactions}) => {
    
    // const sumPoints = Object.values(transactions).reduce((a, b) => a + b);
    // let allPoints = sumPoints(transactions)
    console.log('gpb transactions', transactions)
  }


  export default PointsBalance;

  // getPointsBalance()